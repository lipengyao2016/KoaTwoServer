/**
 * Created by licp on 2016-08-15.
 */
exports.outputFuncCB = function(err,result)
{
    var resultCode,msg;
    var data ;
    if( err ){
        console.log( err );
        resultCode = '9009';
        data = err;

    }else{
        resultCode = 0;
        data = result;
    }

    var response = {
        retCode:resultCode,
        data:data
    };

    //console.log( response );
    var responseJson = JSON.stringify(result);
    console.log( 'responseJson :' + responseJson );

    return responseJson;


};